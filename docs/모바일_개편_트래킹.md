# 모바일 개편 진행도 트래킹 (Toss 이상 품질 목표)

> 대상: 패널리스트 모바일 앱([PanelistMobile](../apps/frontend/src/features/PanelistMobile.tsx) + `.pm-*` 스타일 [App.css](../apps/frontend/src/App.css)).
> 검수자: 박소연(시니어 프로덕트 디자이너, ex-Toss) · 강지후(UX 리서처).
> 검증: 브라우저 자동 클릭은 로그인 연속 애니메이션으로 타임아웃 → lint/build/get_errors/코드 추론으로 검증.
> 원칙: 신규 헤비 의존성 0, 순수 CSS·자체 SVG 우선, 오버엔지니어링 지양, 로직(API·a11y) 보존.

## 전체 페이지(스테이지) 인벤토리

> ⚠️ 2차 크리틱에서 **진입 플로우 전체가 누락**돼 있었음을 발견. 모바일 진입점은 [PanelistApp](../apps/frontend/src/features/PanelistApp.tsx)(로그인→온보딩→부화→앱)이며, 인증 후 [PanelistMobile](../apps/frontend/src/features/PanelistMobile.tsx)로 전환된다. 아래 표는 **두 파일 전체**를 포함하도록 보강했다.

| 코드 | 스테이지/영역                                | 파일·진입                 | 비고               |
| ---- | -------------------------------------------- | ------------------------- | ------------------ |
| L    | 로그인(OAuth 3종·알 히어로)                  | PanelistApp · 최초        | 🔴 신규 발견       |
| OB   | 온보딩(출생연도→성별→관심사 3스텝)           | PanelistApp · 미온보딩    | 🔴 신규 발견       |
| H    | 부화(알 깨우기·웰컴보너스·CTA)               | PanelistApp · 온보딩 직후 | 🔴 신규 발견       |
| SH   | 앱 셸(topbar·bottomnav·toast·overlay)        | 공용 · 상시               | progress 변형 포함 |
| LD   | 로딩(스피너·스켈레톤)                        | 양 파일 · 데이터 대기     | 신규 명시          |
| E    | 빈 상태(덱·참여·정령 3종)                    | PanelistMobile · 데이터 0 | 신규 명시          |
| ERR  | 에러 토스트(API 실패, role=alert)            | 양 파일 · 실패            | 신규 명시          |
| W    | 경고 배너(어뷰즈·오프라인 복구)              | PanelistMobile · 감지     | 신규 명시          |
| D    | 덱(설문 리스트: 헤더·히어로·피드행)          | PanelistMobile · 탭=설문  | 메인               |
| T    | 참여(빠른설문 카드·신청현황·리서치)          | PanelistMobile · 탭=참여  | 상태배지 9종       |
| P    | 정령(scene·진행바·조각·sick·streak·터치대화) | PanelistMobile · 탭=정령  | 변형 다수          |
| S    | 상점(잔액·기프티콘 그리드·교환내역)          | PanelistMobile · 탭=상점  | 벤더색 3종         |
| R    | 응답(single·multi·likert·text·CTA)           | PanelistMobile · 응답 중  | 질문유형 4종       |
| C    | 완료(버스트·포인트·요약·CTA)                 | PanelistMobile · 완료 시  |                    |
| M    | 바텀시트(중단확인·차단안내+카운트다운)       | PanelistMobile · 모달     | 실시간 타이머      |

## 크리틱 → 개편 체크리스트

> 상태: ☐ 미착수 · ◐ 진행 · ☑ 완료. 우선순위 P0(치명)·P1(중)·P2(폴리시).

### M-라운드 2 (2차 정밀 크리틱)

| ID   | 영역     | 우선 | 크리틱(현상태 → 갭)                                                                                                             | 개편                                                                     | 상태 |
| ---- | -------- | ---- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---- |
| M2-1 | SH/T/P/S | P0   | `.pm-btn--sm` 클래스가 JSX 4곳(신청·먹이기·교환·바로가기)에서 쓰이는데 **CSS 미정의** → 보조 버튼이 기본 52px로 과대, 위계 붕괴 | `.pm-btn--sm`(40px·13px·radius 12px) 추가                                | ☑    |
| M2-2 | SH       | P1   | 하단탭 액티브가 색상만 바뀜(배경 pill 없음), 탭 누름 피드백 없음                                                                | 액티브 pill 배경 + `:active` scale + svg 굵기 변화                       | ☑    |
| M2-3 | D        | P1   | `.pm-hero:active`가 scale만(그림자 그대로) → 눌림 깊이감 약함. `.pm-feed-row:active` 1단계 색상만, chevron 정적                 | active 시 그림자 축소 + 행 scale·primary-soft + chevron 이동             | ☑    |
| M2-4 | S        | P2   | 상점 카드 자체에 press 상태 없음, 네이버페이·스벅 벤더색이 둘 다 녹색계라 구분 약함                                             | 카드 `:active` lift, 스벅 벤더색 올리브로 분리                           | ☑    |
| M2-5 | SH 전역  | P1   | `.pm-*` 인터랙티브 요소에 `:focus-visible` 링이 전무 → 키보드/스위치 접근성 누락                                                | 공용 `:focus-visible` 포커스 링 추가(reduced-motion 무관)                | ☑    |
| M2-6 | D/T/P/S  | P2   | 스테이지·리스트 진입 애니메이션이 응답 스테이지에만 있음 → 탭 전환이 밋밋                                                       | 스테이지 fade-up 진입 + reduced-motion 가드                              | ☑    |
| M2-7 | R        | P1   | 리커트 dot이 명시적 min 크기 없음(좁은 폰트스케일에서 48px 미만 위험), 선택지 bullet 선택 시 팝 애니메이션 없음                 | dot `min 52px` 보강 + bullet pop 애니메이션 + choice active primary-soft | ☑    |

> R 영역(M2-7)은 G-9에서 a11y 일부를 처리. 잔여(터치 타깃 명시·bullet pop)는 본 라운드에서 마저 반영.

### M-라운드 3 (문서 재크리틱 — 누락 페이지·기능 발견)

> 트래킹 문서 자체를 재검수한 결과, **진입 플로우(로그인·온보딩·부화)와 다수 상태 화면이 인벤토리에서 통째로 누락**돼 있었음. 더 심각한 건, 누락됐던 공용 셸([PanelistApp](../apps/frontend/src/features/PanelistApp.tsx))이 G-6에서 제거한 줄 알았던 **흰 박스 "잇츠미" 헤더를 그대로 사용** 중이라는 점(G-6 회귀/미완). 아래는 발견 즉시 등재한 후속 개편 항목.

| ID   | 영역            | 우선 | 크리틱(현상태 → 갭)                                                                                                                                    | 개편                                                          | 상태 |
| ---- | --------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------- | ---- |
| M3-1 | 문서            | P0   | 진입 플로우(L/OB/H)·상태화면(LD/E/ERR/W)이 인벤토리에 **전무**                                                                                         | 인벤토리에 7개 코드 추가·보강                                 | ☑    |
| M3-2 | L/OB/H (SH)     | P0   | `PanelistApp` topbar가 옛 `pm-topbar__title`("잇츠미")+`pm-topbar__spacer` 구조 → **사용자가 빼라던 흰/검은 박스 헤더가 진입 화면에 잔존**(G-6 미적용) | G-6 브랜드 락업(SVG 로고+`itsme`) 셸을 `PanelistApp`에도 적용 | ☑    |
| M3-3 | H (부화)        | P1   | 부화 화면이 자체 SVG 정령([PetCreature], G-7) 대신 옛 `pm-hatch__spirit` 블롭 div 사용 → 캐릭터 일관성 깨짐                                            | 부화 정령을 `PetCreature`로 교체(블롭 CSS 제거)               | ☑    |
| M3-4 | L/OB 로딩       | P2   | `PanelistApp` 로딩이 평문 스피너+"불러오는 중..."(G-8 스켈레톤 미적용)                                                                                 | 진입 로딩 품질 보강                                           | ☐    |
| M3-5 | OB (온보딩)     | P2   | 진행 점을 `ProgressDots` 컴포넌트가 아닌 `pm-dot` 직접 마크업으로 중복 구현                                                                            | 공용 컴포넌트로 통일(컴포넌트 추출 필요)                      | ☐    |
| M3-6 | 전역 (다크모드) | P0   | OS 다크모드 시 라이트 프레임 + 다크 토큰 충돌 → **검은 박스 헤더·검은 칩·안 보이는 밝은 폰트**(반쪽 다크)                                              | `.pm-root` 단일 라이트 테마 토큰 락(다크 미디어쿼리 면역)     | ☑    |
| M3-7 | OB (출생연도)   | P0   | 출생연도 60개를 4열 그리드로 전부 노출 → 토스가 경계한 "긴 목록 인지보조 없음" 안티패턴                                                                | 스크롤 스냅 휠 + 중앙 밴드 + 상하 페이드 마스크로 교체        | ☑    |
| M3-8 | 전역 (토큰)     | P1   | `--color-line`·`--color-ink-soft` 미정의 → 칩 테두리/보조 텍스트 깨짐(invalid value)                                                                   | `:root`+`.pm-root`에 토큰 별칭 정의                           | ☑    |

> M3-1·2·3·6·7·8 + M3-4·5 모두 완료(lint·build 통과). 상세 근거는 [토스 UX/UI 방법론 연구](토스_UXUI_방법론_연구.md) §4 안티패턴 매핑 참조.

### M-라운드 4 (메인 앱 스테이지 정밀 크리틱)

> 인증 후 메인 앱([PanelistMobile](../apps/frontend/src/features/PanelistMobile.tsx))의 빈 상태·접근성·터치 타깃을 코드 단위로 재검수. 서브에이전트 감사 결과를 **직접 검증**해 실제 결함만 채택(섹션 단위 빈 상태에 전체화면 CTA 강요 등 과한 제안은 기각).

| ID   | 영역     | 우선 | 크리틱(현상태 → 갭)                                                                           | 개편                                               | 상태 |
| ---- | -------- | ---- | --------------------------------------------------------------------------------------------- | -------------------------------------------------- | ---- |
| M4-1 | S (상점) | P1   | `sortedCatalog`가 0개일 때 "기프티콘으로 교환" 헤더 아래 **빈 상태 전무**(메시지·아이콘 없음) | `pm-emptybox`(Gift 아이콘+안내문) 빈 상태 추가     | ☑    |
| M4-2 | R (응답) | P1   | 단일선택 버튼이 `aria-pressed` 미지정(멀티만 적용) → 스크린리더가 선택 상태 미고지            | 단일선택에도 `aria-pressed={isSelected}` 부여      | ☑    |
| M4-3 | SH (셸)  | P1   | `pm-iconbtn--tight` 닫기 버튼 32×32 → WCAG 44×44 터치 타깃 미달                               | 시각 32px 유지하되 `::after`로 44×44 탭 영역 보장  | ☑    |
| M4-4 | P (정령) | P2   | 정령 레벨·시듦 상태가 동적 변경되나 `aria-live` 없음 → 레벨업/회복 무음                       | `.pm-pet__meta`에 `aria-live=polite`+`aria-atomic` | ☑    |

> M4-1~4 본 턴 완료(lint·build 통과). 기각된 제안: Tasks/Pet 섹션 빈 상태 CTA(섹션 스코프라 전체화면 CTA 불필요), 로고 SVG 색 토큰화(브랜드 고정색이라 의도적).

### M-라운드 5 (실화면 QA — 정령·상점 시각 결함)

> 정령/상점/완료 화면을 폰 프레임 하네스(384px)로 렌더해 **실제 스크린샷으로 QA**. 라이트·다크 양쪽에서 검증한 시각 결함 3종을 수정.

| ID   | 영역     | 우선 | 크리틱(현상태 → 갭)                                                                                                        | 개편                                                                                                       | 상태 |
| ---- | -------- | ---- | -------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ---- |
| M5-1 | P (정령) | P1   | 진행도 라벨("다음 레벨까지 80 EXP")이 20px 막대 **내부**에 `mix-blend-mode:luminosity`로 겹쳐 채움/트랙 경계서 가독성 붕괴 | 막대 10px로 축소·텍스트 분리 → `.pm-pet__progress` 컨테이너로 라벨을 막대 **아래 우측**에 배치, blend 제거 | ☑    |
| M5-2 | P (정령) | P2   | 통계줄("먹인 데이터 12개 / 대기 2개")이 `steel` 저대비 + `space-between`로 좁은 폭에서 빽빽                                | 색 `ink-soft`로 대비↑, `center`+`flex-wrap`+`gap`으로 호흡 확보                                            | ☑    |
| M5-3 | S (상점) | P1   | 좁은 카드에서 `.pm-btn--sm`("포인트 부족")·`.pm-shop__cost`("4,500 P")가 2줄로 줄바꿈                                      | `.pm-shop__footer` 세로 스택(가격 위·전체폭 버튼 아래)+`white-space:nowrap`로 줄바꿈 제거                  | ☑    |

> 부수 확인: 하네스 본문이 회색으로 보인 현상은 `@media(min-width:769px)`의 데스크톱 딤(`pm-root` 배경 `rgba(15,17,21,.5)`) 아티팩트로, **모바일 실화면은 흰 캔버스 정상**임을 하네스 override로 확인(앱 수정 불필요).

## 진행 요약

| 라운드             | 항목 | 완료 | 비고                               |
| ------------------ | ---- | ---- | ---------------------------------- |
| M2 (2차 정밀)      | 7    | 7    | M2-1~7 전수 반영(lint·build 통과)  |
| M3 (문서 재크리틱) | 8    | 8    | M3-1~8 전수 완료                   |
| M4 (메인 앱 정밀)  | 4    | 4    | M4-1~4 전수 완료                   |
| M5 (실화면 QA)     | 3    | 3    | M5-1~3 전수 완료(라이트·다크 검증) |
