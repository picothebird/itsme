NODE_ENV=development
PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
# When set, AI draft/audit endpoints route to the OpenAI provider.
# Leave blank to keep the deterministic heuristic provider (used by CI/tests).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
